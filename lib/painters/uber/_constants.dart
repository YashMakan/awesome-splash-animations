import 'package:flutter/material.dart';

/// ── Visuals ────────────────────────────────────────────────────────────────
const foregroundColor = Color(0xFFFFFDFF);
const backgroundColor = Color(0xFF020002);

/// ── Global timing  (ms) ────────────────────────────────────────────────────
const animationDuration = 2000;

// background
const bgProgressStart   = 980;
const bgProgressEnd     = 1150;
const bgOpacityStart    = bgProgressStart;
const bgOpacityEnd      = animationDuration;

// foreground
const fgProgressStart   = 366;
const fgProgressEnd     = 850;
const fgOffsetStart     = 350;
const fgOffsetEnd       = 915;
const fgLinesLenStart   = 335;
const fgLinesLenEnd     = 715;
const fgLinesGapStart   = 915;
const fgLinesGapEnd     = 1185;
