// One place for tuning knobs & geometry.
const splashDurationMs = 2000;          // animation length
const logoSide         = 100.0;         // square logo box
const masonryCols      = 2;             // grid columns
const gridMaxExtent    = 3;             // == rnd.nextInt(...) + 1
const gridTileUnit     = 100;           // extent * unit = tile height
